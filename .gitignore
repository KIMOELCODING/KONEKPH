config.js
.env
.env.local
.DS_Store
Thumbs.db
node_modules/
.vscode/
.idea/
*.log

# Admin app (Vite + React)
admin-src/node_modules/
admin-src/dist/
admin-src/public/config.js
admin/

# Marketing app (Vite + React)
marketing-src/node_modules/
marketing-src/dist/
marketing-src/public/config.js
marketing/

# Public marketing site (static) — local Supabase config
public-site/config.js
# Generated incremental-build cache — not source, churns on every build
*.tsbuildinfo

# Cloudflare Pages build output (regenerated every deploy)
dist/

# Plan-mode artifacts (none currently committed)

# Supabase CLI local state
supabase/.temp/

# Storage backup script — local secret + downloaded dumps (never commit)
scripts/storage-backup.local.json
storage-backups/

# Public-site Pages build output (generated by build-public.sh)
dist-public/
