// Copy this file to `config.js` and fill in your Supabase project values.
// `config.js` is gitignored — never commit credentials.
//
// Get these from your Supabase project at https://supabase.com/dashboard
//   Project Settings -> API -> Project URL  (SUPABASE_URL)
//   Project Settings -> API -> Project API keys -> anon/public  (SUPABASE_ANON_KEY)
//
// The anon key is safe to expose to browsers — Row-Level Security policies
// in supabase/migrations/0001_initial_schema.sql enforce access control.

window.SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
