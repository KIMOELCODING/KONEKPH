// Copy this file to `config.js` and fill in your Supabase project values.
// `config.js` is gitignored — never commit credentials.
//
// Same Supabase project as the broker/admin apps. The anon key is browser-safe;
// the marketing role's access is enforced by RLS (supabase/migrations/0029).

window.SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
